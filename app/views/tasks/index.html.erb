<h1>Tasks List</h1>
<ul class="list-unstyled">
<% @tasks.each do |task| %>
  <li>
    <h3> <input type="checkbox" name="completed" id="completed">
    <label for="completed"><%= task.title %></label></h3>
    <p><%= task.details %> - completed: <%= task.completed %></p>
  </li>
<% end %>
</ul>
